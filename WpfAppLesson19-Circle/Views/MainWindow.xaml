<Window x:Class="WpfAppLesson19_Circle.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppLesson19_Circle"
        xmlns:vm="clr-namespace:WpfAppLesson19_Circle.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <StackPanel>
        <TextBlock Text="Введите значение радиуса, мм :"/>
        <TextBox Text="{Binding Path=Number1}"/>
        <Button Content="Вычислить длину окружности."
                Command="{Binding Path=CalcCommand}"/>
        <TextBlock Text="Длина окружности составляет :"/>
        <TextBox Text="{Binding Path=Number2}"/>

    </StackPanel>
</Window>
